# Contributing

TotalSegmentator Wrapper for Macへの関心をありがとうございます。
不具合報告、DICOM互換性の報告、ドキュメント改善、コード変更を歓迎します。

## 最初に確認すること

- 本アプリは研究・教育・検証用です。診断や治療計画を目的とする変更は受け付けません。
- 患者名、生年月日、ID、施設名などの個人情報をIssue、Pull Request、ログへ含めないでください。
- DICOMファイルやCT画像そのものを公開Issueへ添付しないでください。
- 大きな機能追加は、実装前にIssueで目的と範囲を相談してください。

## 不具合を報告する

GitHubのIssueテンプレートを利用してください。再現に必要な情報は次のとおりです。

- アプリのバージョン
- Macの機種、Apple Siliconの種類、macOSのバージョン
- どの画面で、何を押したときに起きたか
- 期待した結果と実際の結果
- アプリの`エラー情報をコピー`で取得した内容を、個人情報がないことを確認したうえで貼り付け

読めないDICOMについては専用テンプレートを利用し、DICOM自体は添付しないでください。

## Pull Request

1. `master`の最新状態から作業ブランチを作成します。
2. 変更範囲を必要最小限にします。
3. 不具合修正には、可能な限り修正前に失敗する再現テストを追加します。
4. 関連テストを実行し、実行したコマンドと結果をPull Requestへ記載します。
5. UIを変更した場合は、患者情報を含まないSampleまたはDEBUG画面で確認します。
6. 新しい依存関係や素材を追加した場合は、ライセンスと配布条件を記録します。

基本テスト:

```bash
python -m unittest discover -s tests
```

macOSアプリ、署名、公証、DMG、実機GPUを使うテストは、Pull Requestごとに
必ず実行できるとは限りません。未実施の項目は明記してください。

## ライセンス

Pull Requestを送信することで、提出した第一者の変更を本プロジェクトの
[Apache License 2.0](LICENSE)で提供することに同意したものとします。
第三者コードや素材を含める場合は、出典とライセンスを明示してください。
